# Synthesis plan — conflict fixture

Paused. `conflicts.md` C1 and C2 are still `unresolved`. This plan is not a merge.

## After the user decides

1. Keep unique titles D1-S1-P1 and D2-S1-P1 as source labels.
2. Place exactly one public launch date, the one the user names, with its own citation (D1-R1 or D2-R1). Do not average 12 March and 19 March.
3. If 12 March is kept, D2-S1-P2 and D2-S1-P3 still need a destination the user sets. The agent will not mark them `included` by picking a side.
4. If 19 March is kept, D1-S1-P2 stays out only if the user sets a contract destination; D1-S1-P3 freeze language must be rewritten only from included blocks, or kept as “Friday before the chosen date” after the user says so.
5. Bibliography keeps both definitions unless the user excludes a source.

No `merged-draft.md` until C1 and C2 have explicit decisions.
